<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account_Users</title>

    <style>
        body {
            background-color: #292929;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            font-family: sans-serif;
            font-size: 15pt;
            color: white;
            padding: 3rem;
        }

        ul {
            width: 100%;
            max-width: 800px;
            padding: 2rem;
            background-color: #353535;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            border-radius: 20px;
        }

        li {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 2rem;
            list-style-type: none;
            text-align: left;
            padding: 1rem;
            background-color: #292929;
            border-radius: 20px;
            position: relative;
        }

        h1 {
            font-size: 17pt;
        }

        .delete {
            background-color: red;
            color: white;
            font-weight: bold;
            border: 0;
            border-radius: 10px;
            padding: .5rem .5rem;
            cursor: pointer;
            position: absolute;
            right: .5rem;
            top: .8rem;
        }
        .btn {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            margin-top: 1.5rem;
        }
        .btn1, .btn2 {
            border: 0;
            cursor: pointer;
            padding: .6rem 1rem;
            border-radius: 10px;
            color: white;
            font-weight: bold;
        }
        .btn1 {
        background-color: dodgerblue;
        }

        .btn2 {
            background-color: seagreen;
        }
    </style>
</head>

<body>
    <h1>Conta dos usuários</h1>
    <span>Contas criadas: <%= getUsers.length %></span>
    <div class="btn">
        <a href="/form"><button class="btn1">Novo E-mail</button></a>
        <a href="/"><button class="btn2">Home</button></a>
    </div>
    <ul>
        <% getUsers.forEach(email => { %>
            <li>
                <span>
                    <%= email.email %>
                </span>
                <span>
                    <form action="/users/delete" method="POST">
                        <input type="hidden" name="email" value="<%= email.email %>">
                        <button type="submit" class="delete">Delete</button>
                    </form>
                </span>
            </li>

            <% }) %>
    </ul>
</body>

</html>